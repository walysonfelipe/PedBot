{
  "name": "botpedidos-0.1",
  "version": "1.0.0",
  "description": "",
  "main": "init.js",
  "scripts": {
    "build": "tsc -p .",
    "dev": "ts-node-dev --inspect --ignore-watch node_modules bot/init.ts",
    "start": "node dist/init.js",
    "db": "json-server --watch db-fake/db.json",
    "test": "ts-node-dev --inspect --ignore-watch node_modules bot/subscribers/database.ts"
  },
  "keywords": [
    "bot"
  ],
  "author": "walyson",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "venom-bot": "^4.0.11"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  }
}
